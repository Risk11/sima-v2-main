export interface LoginTypes {
  id: string;
  name: string;
  email: string;
  token: string;
  permissions: string[],
  nama: string,
  up3: string,

  id_pegawai: number, 
  roles: ("1")[]
  jabatan: string
}